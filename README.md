

## Python scripts which I will be using for creating the remote data module

* sanity.py - `sanity_check()` checks if the requested data is available on Google Earth Engine.

* select_data.py - WIP `select_data()` Creates and filters an image collection from the provided details.
